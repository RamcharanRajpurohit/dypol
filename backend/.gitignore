# Secrets — see the repo-root .gitignore for the rationale. Duplicated here
# so the rules still apply if this directory is ever vendored on its own.
*.pem
*.key
.env
.env.*
!.env.example

.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/
.chroma/
